#include "main.h"
/**
 * add - add to int number
 * @i: the first integer
 * @b: the second integer
 *
 * Return: return the two added numbers
 */
int add(int i, int b)
{
	int a = i + b;

	return (a);
}
